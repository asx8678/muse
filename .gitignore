
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Elixir / Mix
/_build/
/deps/
/muse
# Only ignore esbuild output; CSS/images are source-copied and should be tracked
/priv/static/assets/app.js
/priv/static/assets/app.js.map

# Runtime logs
.muse/

# Build artifacts directory (release build script output)
/dist/

# Node / Playwright (browser smoke only — not required for mix test)
node_modules/
playwright-report/
test-results/
blob-report/
*.trace.zip

# Editor / OS
*.swp
*.swo
*~
.DS_Store
erl_crash.dump
